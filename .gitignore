node_modules/
dist/
reports/
test-results/

# Secrets — kritik
.env
*.local

# Auth — rol bazlı storageState (oturum/cookie; secret içerir, ASLA commit edilmez)
/playwright/.auth/

# Playwright
/playwright-report/
/blob-report/

# playwright-bdd — .feature'lardan üretilen spec'ler (derleme çıktısı, commit edilmez)
/.features-gen/

# OS
.DS_Store

# Claude Code — kişisel ayarlar (ekip ayarı settings.json commit edilir)
.claude/
.codex/
.cursor/
CLAUDE.md
AGENTS.md
.github/copilot-instructions.md

