---
description: Bağlayıcı test otomasyon mimarisi (ADR-0001) — her zaman uygula
alwaysApply: true
---

# Mimari (BAĞLAYICI — ADR-0001)

Tam metin: `docs/adr/0001-test-otomasyon-mimarisi.md` · Ayrıca `AGENTS.md`.
Bu karardan sapmak YENİ BİR ADR gerektirir; tek PR içinde sessizce sapma YASAK.
Çakışma görürsen DUR ve kullanıcıya söyle.

- Runner = **Playwright Test** (yeni testler `*.spec.ts` / `playwright-bdd`).
  `cucumber-js`'e yeni bağımlılık/senaryo ekleme.
- **Ports & Adapters:** test/step somut client'a değil **port arayüzüne** bağlanır;
  adapter **fixture** ile enjekte edilir. Tool-bağlı kod yalnızca `src/adapters/**`.
- **Screenplay YASAK.** Chaining = `src/tasks/**` altındaki kompoze görevler.
- **Kanal izolasyonu:** yalnızca tipli context/store üzerinden.
- **Organizasyon DOMAIN bazlı** (`tests/<domain>/`), kanal bazlı değil.
- Test verisi: factory + benzersiz üretim + üretimle aynı yerde cleanup.
- 10k: çok contract/API + az e2e (test trophy); paralel + CI sharding + retry/quarantine.
